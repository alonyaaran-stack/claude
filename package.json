{
  "name": "meta-creative-analytics",
  "version": "1.0.0",
  "private": true,
  "description": "Meta Creative Analytics Platform for marketing teams",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
